# Damian's SSH Config
# Managed by dotfiles
#
# Platform-specific blocks (GitHub auth, 1Password agent, VM forwarding)
# are appended by home/core.nix based on the target platform.

# =============================================================================
# Global Settings
# =============================================================================
Host *
	ServerAliveInterval 60
	ServerAliveCountMax 3
	AddKeysToAgent yes
