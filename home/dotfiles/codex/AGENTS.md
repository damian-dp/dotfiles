# Global Codex Instructions

## Preferences

- Prefer TypeScript over JavaScript
- Use the package manager already used in the repo (check for lockfiles). Default to pnpm if none is present.
- Follow existing code patterns in the codebase
